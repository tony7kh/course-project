import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export const PublicPage = () => {
  return (
    <Box>
      <Typography>Please login to see a content.</Typography>
    </Box>
  );
};
